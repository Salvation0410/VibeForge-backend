<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yupi.yuaicodemother.mapper.CommunityCommentLikeMapper">
    <select id="selectOneIncludingDeleted" resultType="com.yupi.yuaicodemother.model.entity.CommunityCommentLike">
        SELECT id, commentId, userId, createTime, isDelete
        FROM community_comment_like
        WHERE commentId = #{commentId}
          AND userId = #{userId}
        LIMIT 1
    </select>

    <update id="restoreLikeById">
        UPDATE community_comment_like
        SET isDelete = 0
        WHERE id = #{id}
    </update>
</mapper>
